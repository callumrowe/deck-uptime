# Deck — uptime

[![Uptime CI](https://github.com/callumrowe/deck-uptime/workflows/Uptime%20CI/badge.svg)](https://github.com/callumrowe/deck-uptime/actions?query=workflow%3A%22Uptime+CI%22)
[![Response Time CI](https://github.com/callumrowe/deck-uptime/workflows/Response%20Time%20CI/badge.svg)](https://github.com/callumrowe/deck-uptime/actions?query=workflow%3A%22Response+Time+CI%22)
[![Graphs CI](https://github.com/callumrowe/deck-uptime/workflows/Graphs%20CI/badge.svg)](https://github.com/callumrowe/deck-uptime/actions?query=workflow%3A%22Graphs+CI%22)

<!--start: description-->

External uptime monitoring for [Deck](https://deck.cjhr.dev), running on
[Upptime](https://upptime.js.org).

**Why this repository exists at all.** Everything else that watches Deck runs
inside Deck: a signals worker measuring sync age, memory, disk and latency on a
one-minute clock, and an error reporter posting what throws. Both are processes
on the same host as the thing they are watching, and they share one blind spot
— if the host goes down, Docker stops, or the platform Caddy stops routing,
nothing throws and no threshold is crossed. The Slack channels simply go quiet,
and quiet is also what a healthy night looks like.

This checks from outside, every five minutes, on GitHub's runners. It is the
only signal whose absence cannot be mistaken for good news.

**What it checks.** `https://deck.cjhr.dev/healthz`, which performs a real
database query rather than returning 200 for a booted process. A pass from the
public URL means the host, Docker, Caddy, the app and Postgres are all up —
the whole chain a user depends on, tested the way a user meets it.

**What happens when it fails.** An issue opens in this repository and a message
posts to Deck's uptime channel in Slack. Both resolve themselves when the
service recovers.

<!--end: description-->

## Status

<!--start: status pages-->
<!-- This summary is generated by Upptime (https://github.com/upptime/upptime) -->
<!-- Do not edit this manually, your changes will be overwritten -->
<!--end: status pages-->

Live status page: **https://callumrowe.github.io/deck-uptime/**

## Configuration

Everything lives in [`.upptimerc.yml`](./.upptimerc.yml). The workflows under
`.github/workflows/` are generated from it by the Upptime template and are
overwritten weekly — change the config, not the workflows.

Required repository secrets:

| Secret | Purpose |
| --- | --- |
| `GH_PAT` | Fine-grained token with read/write on Actions, Contents, Issues and Workflows for this repository. The workflows commit results and open outage issues with it. |
| `NOTIFICATION_SLACK` | The literal string `true`. |
| `NOTIFICATION_SLACK_WEBHOOK_URL` | Incoming Webhook for Deck's uptime channel. |

The uptime channel is deliberately its own channel, separate from Deck's sync,
infra, backups and error channels. Muting the chatty one must never mute this
one.

## Licence

Upptime is MIT-licensed. Status data in `history/` is
[CC-BY-4.0](./history/LICENSE).
